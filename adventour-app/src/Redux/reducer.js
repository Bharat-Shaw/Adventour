const initialState={
    isAuth:false,
    isLoading:false,
    data:[],
    theme:false
}

const reducer=(state=initialState, action)=>{
    switch(action.type){
        default:
            return state;
    }
}

export {reducer}